import java.util.*;

public class Solution {
    public int solution(int n) {
        int answer = 0;
        String temp = Integer.toString(n);
        for(int i = 0; i < temp.length(); i++) {
            int num = (int)temp.charAt(i) - '0';
            answer += num;
        }
        return answer;
    }
}
